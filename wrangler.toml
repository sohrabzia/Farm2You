name = "farm2you"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# After running:
#   npx wrangler d1 create farm2you-db
# paste the database_id below.

[[d1_databases]]
binding = "DB"
database_name = "farm2you-db"
database_id = "232b406c-5d15-4e3a-a0c1-d17503a13816"

[vars]
# Override in Cloudflare dashboard or via `wrangler pages secret put SESSION_SECRET`.
# In local dev we fall back to "dev-only-not-secure" via env handling.
